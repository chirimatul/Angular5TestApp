import { UserDivDirective } from './user-div.directive';

describe('UserDivDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDivDirective();
    expect(directive).toBeTruthy();
  });
});
