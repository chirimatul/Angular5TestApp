import { UserCustomDirective } from './user-custom.directive';

describe('UserCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new UserCustomDirective();
    expect(directive).toBeTruthy();
  });
});
